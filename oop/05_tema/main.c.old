#include <crtdbg.h>
#include "service.h"
#include "repository.h"
#include "ui.h"
#include "testare.h"
int main()
{
	run_tests();
	//Lista* repo = create_help_empty();
	//Service* srv = create_service(repo);
	//UI* user_interface = create_ui(srv);
	//run(user_interface);
	//destroy_ui(user_interface);
	_CrtDumpMemoryLeaks();
	return 0;
}
